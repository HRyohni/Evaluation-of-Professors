<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notkahoot">

    <application
        android:allowBackup="true"
        android:icon="@drawable/xxxxx"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"

        android:theme="@style/Theme.NotKahoot">
        <activity android:name=".Status" />
        <activity android:name=".Rezultati" />
        <activity android:name=".Odaberi_profesora" />
        <activity android:name=".Pitanja" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Izradi_racun" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>